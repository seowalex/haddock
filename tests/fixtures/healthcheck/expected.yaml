services:
  myservice:
    healthcheck:
      test:
      - curl
      - -f
      - https://localhost
      - '||'
      - exit
      - '1'
      interval: 1m 30s
      timeout: 10s
      start_period: 40s
      retries: 3
    image: alpine
